#!/usr/bin/env sh
#
# Minimal Gradle wrapper script placeholder.
# If Android Studio asks, regenerate the wrapper via:
#   gradle wrapper
#
echo "Gradle wrapper script placeholder. Open in Android Studio to sync/build."
exit 1

